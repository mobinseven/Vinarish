@page
@model Vinarish.Pages.Trains.EditModel

<h4>Train</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Train.TrainId" />
            <div class="form-group">
                <label asp-for="Train.HeadId" class="control-label"></label>
                <select asp-for="Train.HeadId" class="form-control" asp-items="ViewBag.HeadId"></select>
                <span asp-validation-for="Train.HeadId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Train.OfficerId" class="control-label"></label>
                <select asp-for="Train.OfficerId" class="form-control" asp-items="ViewBag.OfficerId"></select>
                <span asp-validation-for="Train.OfficerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
